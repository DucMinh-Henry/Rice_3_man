import React from "react";
import { SquareCheckBig } from "lucide-react";

const IconCheckDisplay = ({ className }) => {
  return <SquareCheckBig className={`w-8 h-8 ${className}`} />;
};

export default IconCheckDisplay;
