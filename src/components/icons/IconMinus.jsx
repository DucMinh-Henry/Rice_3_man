import React from "react";
import { Minus } from "lucide-react";

const IconMinus = ({ onClick, className }) => {
  return <Minus onClick={onClick} className={className} />;
};

export default IconMinus;
