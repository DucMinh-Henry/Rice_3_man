import React from "react";
import { Plus } from "lucide-react";

const IconPlus = ({ onClick, className }) => {
  return <Plus onClick={onClick} className={className} />;
};

export default IconPlus;
